name: Build and Test

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  run:
    name: Run
    uses: CodeQuality-Uyu/workflows/.github/workflows/reusable-build-test.yml@main
    permissions: write-all
    with:
      solution-name: 'Test.sln'
